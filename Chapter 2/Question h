#include<stdio.h>
#include<string.h>
int main(){
  // finding the absolute value of the interger
  int a;
  printf("enter the interger ");
  scanf("%d",&a);
  if(a==0) printf("kuch nahi ho sakta ");
  else a>0 ? printf("\n%d",a) : printf("\n%d",a*-1);
  return 0;
}
