#include<stdio.h>
int main() {
   // temperature conversion form F to C
   // C=f-32 * 5/9
   int a;
   printf("input the temperature of the city ");
   scanf("%d",&a);
   // now the temperature after conversion
   printf("in C is : %d ",(a-32)*(5/9));
    return 0;
}
